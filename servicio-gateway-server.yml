server:
  port: 7000
spring:
  cloud:
    gateway:
      routes:
      - id: servicio-personas
        uri: lb://servicio-personas
        predicates:
          - Path=/api/personas/**
eureka:                  
 client:
  serviceUrl:
   defaultZone: http://localhost:8761/eureka  
